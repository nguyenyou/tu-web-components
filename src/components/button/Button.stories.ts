import type { Meta, StoryObj } from '@storybook/web-components-vite';
import './button.js';

const meta: Meta = {
    component: 'tu-button',
    tags: ['autodocs'],
  };
  
  export default meta;
  
  type Story = StoryObj;
  
  export const Default: Story = {};